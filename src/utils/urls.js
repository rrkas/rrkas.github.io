export const urls = {
    root: "/",
    // model: {
    //   root: "/model",
    //   adapt: (id) => `${urls.model.root}/${id}/adapt`,
    // },
    // tenant:{
    //   root: "/tenant",
    //   details: (id) => `${urls.tenant.root}/${id}`,
    // }
  };
  